1. 怎么改ap的mode bridge/clear/sniffer/monitor/se. AP 下。但是说site tag里面也有可以设置的地方在哪里。
2. 怎么改ap的type，有没有支持多种type的ap
3. 天线和band的对应表。一个天线都能支持两个band吗？只支持single band的天线接口，给的rf tag确还是两个band都支持的好像，有时候有dual band选，有时候都没选。看上去跟ap type有关系？
4. ap join profile.是设置ap 和ac之间的一些参数dtls是 ac ap数据加密的方式。EAP是ap和ac之间认证报的格式，ac和radius server之间也是eap格式嘛？
5. 如果ap网络里有两个wlc，一个ap为什么不能备份两个wlc
6. ap 由 ac 上的dhcp 分地址。 能不能用外部的dhcp。 sta 由什么分地址，是怎么在ap上配置的。 
7. bridge mode是不是就是mesh mode， mesh mode就是 ap分几个wlan出来专门给别的ap进行连接嘛，改成bridge mode还允许 sta接入嘛？ 相对于local和flex mode sta的dhcp地址是不是变成和ap同一个网段。
8. 像这种bridge mode为什么不是基于wlan的，这样分几个wlan给别的ap连接进来就好了，不用整个ap都改成bridge啊。哈
9. flex 模式下， local assoicsation， central authorize， switch 具体怎么配置。好像在wlan上有配置local authentication， profile里面也有local radius authority。
10. 抓包怎么配置。
11. flex bridge 模式。

